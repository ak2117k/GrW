fastapi==0.115.0
uvicorn[standard]==0.32.0
pandas>=2.0
numpy>=2.0
pydantic>=2.10.0
httpx==0.28.0
redis==5.2.0
python-dotenv==1.0.0
websockets==13.0
# Pattern-quality model (Phase 2): XGBoost classifier + sklearn for calibration/metrics.
xgboost>=2.1
scikit-learn>=1.5
# Telegram signal parser (Phase 1): Claude Agent SDK — subscription-auth LLM backend
# (host Claude Code login), NOT an ANTHROPIC_API_KEY. Swappable seam in _call_llm.
claude-agent-sdk==0.2.124
